type Project {
    title: String!
    rank: Int
    url: String!
    points: Int
    username: String!
    comments: Int
}

type Mutation {
    putProject(data: ProjectInput!): Result
    deleteProject(title: String!): Result
}